# Nixpacks configuration for Railway
# https://nixpacks.com/docs/configuration/file

[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
# Build the Vue.js frontend during build phase
cmds = ["npm run build"]

[start]
# Start command will be set via Railway environment or Procfile

