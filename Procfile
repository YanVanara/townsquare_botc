# Procfile for Railway deployment
# This file tells Railway how to run your app

# For WebSocket server deployment:
web: cd server && NODE_ENV=production PORT=${PORT:-8080} node index.js

# For Frontend deployment (comment out the line above and uncomment below):
# web: npm run serve

